---
cssclasses: [wb-page]
obsidianUIMode: preview
---

```workbench
```

<!--
这是插件版的首页，和「工作台.md」长得一模一样，区别只在这一行代码块：

  插件版      ```workbench      装了 Workbench Kit 插件即可，不需要 Dataview
  dataviewjs  ```dataviewjs     需要 Dataview 且开启 JavaScript 查询

同一套内核、同样 51 个组件、同样 12 套主题，两边渲染结果一致。
选哪条路都行，不必两个都装。

怎么用：
1. 点右上角「✏️ 编辑」进入编辑态
2. 点「＋ 组件」挑组件，它会自动找一个空位放进去
3. 拖标题条移动，拖右下角缩放，点 ⚙ 改参数，点 ✕ 删除
4. 点「🎨 主题」一键切换全部组件的配色
5. 改完再点一次「✏️ 编辑」退出，首页就恢复成干净的样子

代码块里可以写 YAML 覆盖参数，例如只放一个热力图：

  ```workbench
  c: heatmap
  weeks: 26
  ```

留空（就像上面那样）表示整块看板，布局存在插件目录下的
.obsidian/plugins/workbench-kit/boards/<笔记名>.json —— 注意这一点和
dataviewjs 版不同，后者存在库里的 _wb/boards/ 下。
-->
