# 更新日志

## 0.4.2 · 配色

针对「有些配色看起来不够高级、很单薄」和「深色主题的文字不清楚」两条反馈重做了整个配色层。新增 2 套主题，共 12 套。

**深色底的文字**

WCAG 2 的对比度公式在深色底上系统性高估可读性 —— 同样 4.6:1，浅底上的灰字清清楚楚，深底上的灰字发糊。WCAG 3 草案里的 APCA 算法就是为这件事提出的。我没有凭记忆实现 APCA（写错一套感知模型比不做更糟），而是给深色底加一个约 1.5 倍的经验倍率：正文 7→13、次要 4.5→9、弱化小字 4.5→7、强调色 3→4.5。

按新门槛一量，**全部 9 个深色变体的次要文字和弱化小字都不达标**，报头小字更是每套主题都只有 4.6 —— 而报头底一律是深色。原因是生成脚本取的是「仍然达标的最弱值」，门槛定低了，取出来的就是刚好糊的那一档。

**深色主题改为边框分层**

深底上硬提卡片明度，会把「正文 / 卡片底」的对比度一起压下去。所以深色主题不再靠明度阶分层，改成：卡片底贴近页底、边框明确可见（≥1.35）、加实投影。这是取舍，写进了审计判据 —— 两条路走通一条即可。

**双色相强调色**

配色「单薄」的直接来源：`--wb-accent-2` 一直是主强调色调亮一档，同一个色相的深浅画成渐变几乎看不出变化，条形图、柱状图、热力图因此全是平的。现在的强调色对取自中国传统色的两两配对 —— 茶色×藕荷、铭金×竹子青、鱼师青×青梅、天青×鹅黄、绀宇×十样锦、绿茶×天水碧 —— 色相差 26°～176°。原色不能直接当文字用（铭金色压在米白纸上只有 1.3:1），脚本保持色相与饱和度、只推明度到达标为止。

素笺是故意的例外：它只能拿到用户 Obsidian 的强调色，而 Obsidian 的 accent 家族全是同一色相的深浅。

**新增主题**

- **绀宇**（浅色 + 深色覆写）—— 十样锦的暖粉纸压绀青深蓝。其余浅色主题的底不是白纸就是米黄，这是唯一一套「有颜色的浅底」；深色模式下两色角色对调，蓝底衬粉字。
- **沉绿**（认定深色）—— 沉绿夜底配绿茶色，整套里唯一敢用高饱和亮色的。沉绿原色当不了卡片底：它的相对亮度 0.026，正文压上去顶多 11.8:1，连纯白都只有 12.5:1，过不了深色底那条线；所以底色整摞下压一档，原色留给热力图空格 —— 那是整页上这个颜色面积最大的地方。

**修复**

- **印章白文对比度不足。** `seal` 把页底色当白文印在强调色实心块上，但生成脚本只保证强调色「当图形色看得见」（3.2:1）。鱼师青、竹子青这类中明度传统色正好卡在线上，青瓷 4.13、竹影 4.22。现在强调色对页底另有一条 4.6 的约束。
- **审计凭空多出一个「松烟 深」变体。** 它按字符串 `.theme-dark` 找深色覆写块，而松烟的注释里正好写着「没有 .theme-dark 覆写」。改成认选择器。读数和浅色那行一模一样本该早点引起怀疑。
- README 里的「门槛 / 实测最低」表以前是手抄的，改配色就悄悄过期。现在由 `audit-contrast.mjs` 直接打印。
- 修正 README 中已过期的组件数（31 → 51）与主题数。

## 0.4.1 · 历法

新增 `lunar` 农历组件，并把节气重写到同一套天文算法上。

**新增**

- `core/astro.js` —— 历法计算内核：儒略日换算、朔望（Meeus 第 49 章，含 14 项行星摄动）、太阳视黄经、24 节气、农历排月（含无中置闰）、干支生肖。日界一律按北京时。
- `lunar` 农历组件：农历日期、干支生肖，可带当前节气与春节倒数。
- `scripts/verify-lunar.mjs` —— 历法自检。**加载真正发布的那个文件**，不是抄一份副本；抄一份就变成「验证副本」，改了源文件也不会被发现。

**为什么用天文算法而不是查表**

查表要写两百个十六进制常数，我凭记忆写出来出错很难发现，而**农历算错会安静地显示在首页上，没人会察觉**。天文法每一步都能拿公认日期对：11 个春节（2020–2030）、3 个闰月位置（2020 闰四月 / 2023 闰二月 / 2025 闰六月）、7 个农历节日（春节、元宵、七夕、中秋）、168 个节气 —— 全部通过才落地成组件。

**修复：节气在闰年整体差一天**

原先的 `solar-term` 用「通用寿星公式」，它的 L 项漏了闰年修正 —— **2024、2028 这类闰年从惊蛰（3 月）到冬至，24 个节气里有 20 个差一天**。这个 bug 是天文法和寿星公式交叉校验时暴露的：两法在 2024 全年分歧 20 处，补上闰日修正后降到 0。

现在节气直接用天文法，这个问题连同寿星公式本身的已知特例（如 2026 年雨水）一起消失。7 年 168 个节气里两法仅剩 1 处分歧，且分歧的是寿星公式那边。

**顺带**

- 内核新增 `astro` 模块，组件通过 `ctx.astro` 使用。

## 0.4.0 · 组件扩充

31 → **50 个组件**，新增「装饰」分类。

**实用组（13 个）**

`table` 表格（自选字段当列，最大的能力空白）· `kanban` 分列看板（按字段或文件夹）· `week` 本周 · `habit` 习惯打卡（读日记里的 checkbox，不用另记一份）· `on-this-day` 那年今日 · `countdown` 倒计时 · `stale` 久未回顾 · `coverage` 字段体检 · `opened` 最近打开（按实际打开顺序，≠ 修改时间）· `reading` 在读进度 · `attachments` 附件体检 · `timeline` 时间线 · `graph` 关系图（纯 SVG 径向布局，不引外部库）

**装饰组（6 个）**

`seal` 印章 · `poem` 竖排诗词 · `solar-term` 节气 · `year-progress` 时间进度 · `analog-clock` 表盘 · `image` 图片/图集

装饰组件同样只用 `--wb-*` 变量，所以印章在青瓷主题下是青的，在朱砂下才是红的。

**实现上的几个取舍**

- **节气纯计算**，用通用寿星公式，不联网也不需要数据表。已对照 2025/2026 历表验证（立春 2/3 与 2/4、清明 4/4 与 4/5、冬至 12/21 与 12/22 均一致）。个别年份有 ±1 天的已知特例，够首页展示用，但别拿它排盘。
- **关系图用径向布局而不是力导向**：力导向要跑迭代和动画，在首页上不划算，而且每次刷新位置都不一样反而难认。环形是确定的 —— 同一批笔记永远画在同一位置。
- **看板只读不写**：拖卡片改 frontmatter 听起来很美，但一旦格式不合预期就会毁掉整个文件头。
- **附件体检加了扩展名白名单**：「非 md 文件」不等于「附件」，否则 `_wb/` 下的 js/css 全会被列出来。

**修复**

- `seal` 的印文分列写反了，「知之为知之」排成了「知知/之之/为」。印文的读序是自右向左、自上而下，两列应该是前半在右列，而不是交错填充。

## 0.3.0 · 插件版

同一套内核和 31 个组件，现在能以 Obsidian 插件的形式运行，**不再依赖 Dataview**。

**新增**

- `plugin/` —— Obsidian 插件外壳：` ```workbench ` 代码块（YAML 配置）、侧边栏图标、命令、设置界面（主题 / 语言 / 路径别名 / 配色微调 / 重建索引）。
- `core/query-native.js` —— 直接读 Obsidian `metadataCache` 的数据层，和 Dataview 版接口完全相同。反链从 `resolvedLinks` 反转一次得到；任务索引只读那些确实含 checkbox 的文件（缓存里就能判断，大库能省掉绝大部分读盘）；勾选回写前先核对原行内容，对不上就放弃并重建索引。
- `scripts/build-plugin.mjs` —— 把 `_wb/` 全部内联成单个 `main.js`（82 个资源 / 251 KB）。**不用 esbuild、不用 npm**：内核本来就是「读源码字符串再求值」的结构，打包只是把文件内容换成一个对象字面量，为此装一整套 node_modules 不划算。
- `dev/plugin-test.html` —— 插件模式测试台。桩掉 `require('obsidian')` 后真的调 `onload()` 和代码块处理器，22 项断言。语法检查只能说明能解析，这里验证的是能不能跑。
- 预览器加入「数据层」开关，同一批数据让 Dataview 版和原生版逐项对比。

**内核重构**

- 内核不再直接摸 vault。列组件/主题/模板、读资源、存布局全部收进 `WB.host` —— dataviewjs 模式落到 vault 文件，插件模式落到打包资源。**31 个组件一行没改。**

**修复**（都是换数据层才暴露的）

- `notes` 组件排序会污染 `pages()` 返回的缓存数组，导致后面所有按下标取值的组件错位。Dataview 的 `.array()` 每次返回新数组所以一直没事。现在契约明确：**`pages()` 返回的数组归调用方所有**。
- `hubs` / `upcoming` / `recent` / `notes` / `orphans` / `untagged` 在并列或时间相同时顺序不定，换个数据层就得出不同结果。全部加了按路径的兜底排序。
- 两套数据层的页面顺序统一按路径排序 —— 从 dataviewjs 版切到插件版，首页长得一模一样。实测 30 个组件里 29 个输出完全一致，剩下一个只是 DOM 空白差异。

## 0.2.1

修的都是只有在真实 Obsidian 里才会暴露的问题 —— 浏览器预览器没有 Obsidian 的全局样式，这些全被掩盖了。

**修复**

- 组件面板里的文字互相重叠。Obsidian 给 `button` 设了固定 `height`，而面板条目里是三行块级内容，撑不开就溢出压到下一条。加了表单控件重置，特意用 `.wb button`（特异度 0,1,1）而不是 `:where()` —— 后者和 Obsidian 的规则打平，谁后加载谁赢，等于赌加载顺序。同一成因也影响日记入口、快捷入口、搜索框、模板卡片。
- 拖右下角缩放时块会自己跑掉位置。缩放误用了拖动的布局算法，那个算法给被操作的块最高重力优先级，一改尺寸就往上浮。新增 `board.resize()`：被改尺寸的块钉在原地，只让邻居让路；宽度只在右边界截断，绝不反推 x。
- 拖拽精度：加了指针捕获（移出元素不丢事件）、缩放手柄命中区 16→22px、拖动时禁用文字选中。
- 浮层会伸出屏幕外。面板内容是异步填的，首次定位时量到的是空面板。改成填完后显式重新定位；`ResizeObserver` 只作兜底 —— 它的回调要等渲染帧，窗口隐藏时根本不触发。
- 模板套用后主题不生效：模板里写的是中文名「朱砂」，未归一就拼进了 CSS 类名 `wb-theme-朱砂`，和 `.wb-theme-zhusha` 对不上。

**新增**

- 主题名支持中文：`config.json`、frontmatter `wbTheme`、模板里都可以写「宣纸」「松烟」，拼音 id 和英文名等价。
- 预览器加入「模拟 Obsidian 样式」开关（默认开），把 Obsidian 对 `button` / `input` / `ul` 的全局样式按真实加载顺序注入，这类 bug 从此在浏览器里就能抓到。
- 预览器加入字体自检：逐像素检测各字体族在本机实际落到哪一个（量文字宽度不行 —— 汉字在几乎所有字体里都是等宽全角）。

---

## 0.2.0

**主题体系重做**

10 套中式命名主题，配色由 `scripts/gen-themes.py` 统一生成（手写十份必然漂移）：

素笺 · 宣纸 · 水墨 · 朱砂 · 青瓷 · 秋毫 · 竹影 · 松烟 · 靛蓝 · 曜石

- 字体族库沉到基础层（`--wb-f-song / -kai / -hei / -yuan / -fangsong / -serif / -mono`），一律只用系统已装字体，不联网拉。
- 主题自我描述：名字、说明、字体、三色样本写在各自 CSS 的头部注释里，丢一个 `.css` 进 `themes/` 就自动出现在菜单。
- **每套主题过对比度门槛**。第一版凭感觉配的 10 套里有 6 套没达标，返工三处：浅色主题的弱化文字普遍只有 2.3–2.7；秋毫的金色对奶油底只有 2.52（改配墨字）；深色模式下强调色变浅但 `on-accent` 还留着白色，最低 2.24。最终 16 个变体全部通过。

**新增**

- 4 套开箱模板（极简起步 / 知识库 / 日程计划 / 写作台），空看板直接摆出卡片，点一下铺好整页。
- 撤销（最多 20 步），删错组件不用重搭。

**修复**

- 撤销失效：套模板会顺带换主题，而换主题也压了一份快照，导致点撤销只退掉主题、退不回布局。
- 模板套用后组件内容被切：模板里的高度是手写估值，现在按实际内容重新定高。

---

## 0.1.0

**首个可用版本**

- 运行时内核：单一入口调度器、组件契约、路径别名、错误边界、样式自动注入（不需要装 CSS 片段）。
- 可视化看板：拖拽移动、缩放、加删组件、参数表单（由 `meta.props` 自动生成）、主题切换、布局持久化到 `_wb/boards/<笔记名>.json`。
- 31 个组件，分 6 类：页头 / 导航入口 / 数据统计 / 笔记流 / 任务与计划 / 版面元素。
- 组件面板按分类分组，支持搜索。

**过程中修掉的两个布局引擎 bug**

- 路径别名正则只认 ASCII，中文别名会被静默当成普通路径查出空结果 —— 对中文优先的库来说是最难排查的失败方式。
- 布局压实只检查「能不能往上浮」，从不检查当前位置本身是否已经撞上别人，导致两个块能同时停在 y=0。改成「先下沉、再上浮」两步。
